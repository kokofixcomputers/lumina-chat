VITE_VERCEL_GIT_COMMIT_SHA=dev

# Required for Tauri desktop builds only.
# Set this to your Vercel deployment URL so OneDrive OAuth can reach the token proxy.
# Leave blank (or unset) for web builds — the proxy is reached via relative path.
VITE_APP_URL=https://lumina-chat-rho.vercel.app